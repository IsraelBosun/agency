/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["flowbite.s3.amazonaws.com", 'images.unsplash.com', 'cdn.flames.design', 'cdn.dribbble.com', 'drive.google.com'],
  },
};

export default nextConfig;
